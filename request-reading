<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Begin Your Reading — Venicit Private Client Portal</title>
  <meta name="description" content="A private space for Venicit clients who have completed a purchase. Enter your access code to begin your reading request.">
  <meta name="robots" content="noindex, nofollow">

  <link rel="icon" type="image/png" href="../favicon.png">
  <link rel="canonical" href="https://venicit.com/request-reading/">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,500;1,9..144,500&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
/* ==========================================================================
   Venicit — Design System
   (mirrored from the main site so this page shares one visual identity)
   ========================================================================== */

:root {
  --ink-black: #150c12;
  --plum-950: #1a0f17;
  --plum-900: #241420;
  --plum-800: #2c1826;
  --plum-700: #3a2032;
  --gold-400: #d9bd83;
  --gold-500: #c9a962;
  --gold-600: #a8863f;
  --sage-500: #4c6152;
  --sage-400: #63795f;
  --ivory-100: #f7f2ea;
  --ivory-300: #ede6dc;
  --ivory-500: #cabfae;
  --line-hair: rgba(201, 169, 98, 0.22);
  --shadow-deep: rgba(8, 4, 6, 0.55);

  --font-display: "Fraunces", "Iowan Old Style", Georgia, serif;
  --font-body: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;

  --space-1: 0.5rem;
  --space-2: 1rem;
  --space-3: 1.75rem;
  --space-4: 2.75rem;
  --space-5: 4.5rem;
  --space-6: 7rem;
  --radius-sm: 2px;
  --radius-md: 4px;
  --max-width: 1240px;

  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
}

*, *::before, *::after { box-sizing: border-box; }
html { scroll-behavior: smooth; }
@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

body {
  margin: 0;
  background: var(--plum-950);
  color: var(--ivory-300);
  font-family: var(--font-body);
  font-size: 16px;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}

img { max-width: 100%; display: block; }
a { color: inherit; text-decoration: none; }

h1, h2, h3, h4 {
  font-family: var(--font-display);
  font-weight: 500;
  color: var(--ivory-100);
  line-height: 1.15;
  margin: 0;
}

p { margin: 0; }

::selection { background: var(--gold-600); color: var(--plum-950); }

:focus-visible {
  outline: 2px solid var(--gold-500);
  outline-offset: 3px;
  border-radius: var(--radius-sm);
}

.wrap {
  max-width: var(--max-width);
  margin-inline: auto;
  padding-inline: var(--space-3);
}

/* Kraft-label eyebrow */
.eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 0.6em;
  font-family: var(--font-body);
  font-size: 0.72rem;
  font-weight: 600;
  letter-spacing: 0.22em;
  text-transform: uppercase;
  color: var(--gold-400);
}

.eyebrow::before {
  content: "";
  width: 22px;
  height: 1px;
  background: var(--gold-600);
}

hr.rule {
  border: none;
  border-top: 1px solid var(--line-hair);
  margin: 0;
}

/* ---------- Buttons ---------- */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.6em;
  padding: 0.85em 1.9em;
  font-family: var(--font-body);
  font-size: 0.85rem;
  font-weight: 600;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  border-radius: var(--radius-md);
  border: 1px solid transparent;
  cursor: pointer;
  transition: transform 0.35s var(--ease-out), background 0.35s var(--ease-out), border-color 0.35s var(--ease-out), color 0.35s var(--ease-out);
}

.btn-primary {
  background: linear-gradient(180deg, var(--gold-400), var(--gold-600));
  color: var(--plum-950);
  width: 100%;
}
.btn-primary:hover { transform: translateY(-1px); filter: brightness(1.06); }
.btn-primary:disabled { opacity: 0.6; cursor: not-allowed; transform: none; filter: none; }

.btn-ghost {
  background: transparent;
  border-color: var(--line-hair);
  color: var(--ivory-100);
}
.btn-ghost:hover { border-color: var(--gold-500); color: var(--gold-400); }

/* ---------- Header ---------- */
.site-header {
  position: sticky;
  top: 0;
  z-index: 50;
  background: rgba(21, 12, 18, 0.82);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--line-hair);
}

.site-header .wrap {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 84px;
}

.brand {
  font-family: var(--font-display);
  font-size: 1.35rem;
  letter-spacing: 0.04em;
  color: var(--ivory-100);
}

.brand span { color: var(--gold-500); }

.nav-links {
  display: flex;
  align-items: center;
  gap: var(--space-4);
  list-style: none;
  margin: 0;
  padding: 0;
}

.nav-links a {
  font-size: 0.85rem;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--ivory-500);
  position: relative;
  padding-block: 4px;
  transition: color 0.3s var(--ease-out);
}

.nav-links a:not(.nav-cta)::after {
  content: "";
  position: absolute;
  left: 0; right: 0; bottom: -2px;
  height: 1px;
  background: var(--gold-500);
  transform: scaleX(0);
  transform-origin: left;
  transition: transform 0.35s var(--ease-out);
}

.nav-links a:hover:not(.nav-cta),
.nav-links a[aria-current="page"] { color: var(--ivory-100); }
.nav-links a:hover:not(.nav-cta)::after,
.nav-links a[aria-current="page"]::after { transform: scaleX(1); }

.nav-toggle {
  display: none;
  background: none;
  border: 1px solid var(--line-hair);
  border-radius: var(--radius-md);
  width: 42px;
  height: 42px;
  color: var(--ivory-100);
  cursor: pointer;
}

/* Begin Reading — premium nav CTA, echoes the "Most Requested" gold badge motif */
.nav-cta {
  display: inline-flex;
  align-items: center;
  gap: 0.5em;
  padding: 0.55em 1.3em;
  background: linear-gradient(180deg, var(--gold-400), var(--gold-600));
  color: var(--plum-950) !important;
  font-family: var(--font-body);
  font-size: 0.78rem;
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  border-radius: 999px;
  box-shadow: 0 6px 18px -6px rgba(201, 169, 98, 0.55);
  transition: transform 0.35s var(--ease-out), box-shadow 0.35s var(--ease-out), filter 0.35s var(--ease-out);
}

.nav-cta svg { flex-shrink: 0; }

.nav-cta:hover {
  transform: translateY(-1px);
  filter: brightness(1.05);
  box-shadow: 0 8px 22px -6px rgba(201, 169, 98, 0.7);
}

/* Ambient candlelight glow */
.glow {
  position: absolute;
  border-radius: 50%;
  filter: blur(90px);
  pointer-events: none;
  opacity: 0.55;
  animation: flicker 7s ease-in-out infinite;
}

.glow--gold {
  width: 420px; height: 420px;
  background: radial-gradient(circle, rgba(201, 169, 98, 0.35), transparent 70%);
  top: -120px; left: -140px;
}

.glow--sage {
  width: 360px; height: 360px;
  background: radial-gradient(circle, rgba(76, 97, 82, 0.4), transparent 70%);
  bottom: -140px; right: -100px;
  animation-delay: 2.4s;
}

@keyframes flicker {
  0%, 100% { opacity: 0.45; transform: scale(1); }
  50% { opacity: 0.65; transform: scale(1.06); }
}

/* ---------- Portal shell ---------- */
.portal {
  position: relative;
  overflow: hidden;
  min-height: calc(100vh - 84px - 1px);
  display: flex;
  align-items: center;
  border-bottom: 1px solid var(--line-hair);
}

.portal .wrap {
  position: relative;
  z-index: 1;
  padding-block: var(--space-6);
  width: 100%;
}

/* ---------- Access gate ---------- */
.gate {
  max-width: 460px;
  margin-inline: auto;
  text-align: center;
}

.gate .eyebrow { justify-content: center; margin-bottom: var(--space-3); }

.gate h1 {
  font-size: clamp(2rem, 4vw, 2.7rem);
  letter-spacing: -0.01em;
}

.gate h1 em {
  font-style: italic;
  color: var(--gold-400);
}

.gate .disclaimer {
  margin-top: var(--space-3);
  color: var(--ivory-500);
  font-size: 0.98rem;
  max-width: 42ch;
  margin-inline: auto;
}

.gate-card {
  margin-top: var(--space-4);
  background: var(--plum-900);
  border: 1px solid var(--line-hair);
  border-radius: var(--radius-md);
  padding: var(--space-4);
  box-shadow: 0 40px 80px -30px var(--shadow-deep);
}

.field {
  text-align: left;
}

.field + .field { margin-top: var(--space-3); }

.field label {
  display: block;
  font-size: 0.72rem;
  font-weight: 600;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--gold-400);
  margin-bottom: 0.6em;
}

.field input,
.field textarea {
  width: 100%;
  background: var(--plum-950);
  border: 1px solid var(--line-hair);
  border-radius: var(--radius-md);
  color: var(--ivory-100);
  font-family: var(--font-body);
  font-size: 0.95rem;
  padding: 0.85em 1em;
  transition: border-color 0.3s var(--ease-out), box-shadow 0.3s var(--ease-out);
}

.field input::placeholder,
.field textarea::placeholder { color: var(--ivory-500); opacity: 0.6; }

.field input:focus,
.field textarea:focus {
  outline: none;
  border-color: var(--gold-500);
  box-shadow: 0 0 0 3px rgba(201, 169, 98, 0.14);
}

.field textarea { resize: vertical; min-height: 110px; }

.gate-card .btn-primary { margin-top: var(--space-3); }

.gate-error {
  margin-top: var(--space-2);
  font-size: 0.85rem;
  color: #d98c8c;
  min-height: 1.2em;
}

.gate-note {
  margin-top: var(--space-3);
  font-size: 0.8rem;
  color: var(--ivory-500);
  font-style: italic;
}

/* ---------- Reading request form ---------- */
.request-panel {
  max-width: 680px;
  margin-inline: auto;
}

.request-panel .section-head {
  text-align: center;
  margin-bottom: var(--space-4);
}

.request-panel .eyebrow { justify-content: center; margin-bottom: var(--space-2); }

.request-panel h1 {
  font-size: clamp(1.9rem, 3.4vw, 2.5rem);
}

.request-panel .subhead {
  margin-top: var(--space-2);
  color: var(--ivory-500);
  max-width: 48ch;
  margin-inline: auto;
}

.request-form {
  margin-top: var(--space-4);
  background: var(--plum-900);
  border: 1px solid var(--line-hair);
  border-radius: var(--radius-md);
  padding: var(--space-4);
  box-shadow: 0 40px 80px -30px var(--shadow-deep);
}

.request-form .field-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-3);
}

.request-form .field-row .field { margin-top: 0; }

.request-form .field + .field-row,
.request-form .field-row + .field { margin-top: var(--space-3); }

.request-form .form-foot {
  margin-top: var(--space-4);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-3);
  flex-wrap: wrap;
}

.request-form .form-foot .btn-primary { width: auto; padding-inline: 2.6em; }

.form-status {
  font-size: 0.85rem;
  color: var(--ivory-500);
}

.form-status[data-state="success"] { color: var(--gold-400); }

/* ---------- Reveal transitions ---------- */
.is-hidden { display: none !important; }

.fade-enter {
  animation: fadeIn 0.6s var(--ease-out) both;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* ---------- Footer ---------- */
.site-footer {
  background: var(--ink-black);
  border-top: 1px solid var(--line-hair);
  padding-block: var(--space-4);
}

.site-footer .wrap {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: var(--space-2);
  font-size: 0.8rem;
  color: var(--ivory-500);
}

.site-footer a:hover { color: var(--gold-400); }

/* ---------- Responsive ---------- */
@media (max-width: 720px) {
  .nav-links { display: none; }
  .nav-toggle { display: inline-flex; align-items: center; justify-content: center; }
  .nav-links.is-open { display: flex; flex-direction: column; align-items: flex-start; gap: var(--space-3); position: absolute; top: 100%; left: 0; right: 0; background: var(--plum-950); border-bottom: 1px solid var(--line-hair); padding: var(--space-3); }
  .nav-links .nav-cta { align-self: flex-start; }
  .request-form .field-row { grid-template-columns: 1fr; gap: 0; }
  .request-form .field-row .field + .field { margin-top: var(--space-3); }
  .request-form .form-foot { flex-direction: column; align-items: stretch; }
  .request-form .form-foot .btn-primary { width: 100%; }
}

@media (max-width: 480px) {
  .site-header .wrap { height: 68px; }
  .portal .wrap { padding-block: var(--space-5); }
  .gate-card, .request-form { padding: var(--space-3); }
}

  </style>
</head>
<body>

  <header class="site-header">
    <div class="wrap">
      <a href="../" class="brand">Venic<span>it</span></a>
      <nav aria-label="Primary">
        <ul class="nav-links">
          <li><a href="../index.html">Home</a></li>
          <li><a href="../about.html">About</a></li>
          <li><a href="../sessions.html">Sessions</a></li>
          <li><a href="../faq.html">FAQ</a></li>
          <li><a href="../contact.html">Contact</a></li>
          <li><a href="./" class="nav-cta" aria-current="page"><svg width="13" height="13" viewBox="0 0 18 18" fill="none"><path d="M9 1L11 6.5L17 7L12.5 11L14 17L9 13.5L4 17L5.5 11L1 7L7 6.5L9 1Z" fill="currentColor"/></svg>Begin Reading</a></li>
        </ul>
      </nav>
      <button class="nav-toggle" aria-label="Toggle menu" aria-expanded="false">
        <svg width="18" height="18" viewBox="0 0 18 18" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M1 4H17M1 9H17M1 14H17" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/>
        </svg>
      </button>
    </div>
  </header>

  <main>
    <section class="portal">
      <div class="glow glow--gold" aria-hidden="true"></div>
      <div class="glow glow--sage" aria-hidden="true"></div>
      <div class="wrap">

        <!-- ACCESS GATE -->
        <div class="gate" id="gate">
          <span class="eyebrow">Private Access</span>
          <h1>Your reading is <em>waiting.</em></h1>
          <p class="disclaimer">This private space is reserved for clients who have completed the purchase. Enter the access code provided after your purchase to begin your spiritual reading assessment.</p>

          <form class="gate-card" id="gate-form" novalidate>
            <div class="field">
              <label for="access-code">Access Code</label>
              <input type="password" id="access-code" name="access-code" placeholder="Enter your code" autocomplete="off" required>
            </div>
            <button type="submit" class="btn btn-primary" id="gate-submit">Unlock My Reading</button>
            <p class="gate-error" id="gate-error" role="alert" aria-live="polite"></p>
          </form>

          <p class="gate-note">Didn't receive a code, or having trouble? <a href="../contact.html" style="color: var(--gold-400); text-decoration: underline;">Reach out to us directly</a>.</p>
        </div>

        <!-- READING REQUEST FORM (revealed after unlock) -->
        <div class="request-panel is-hidden" id="request-panel">
          <div class="section-head">
            <span class="eyebrow">Welcome, Client</span>
            <h1>Let's begin your reading.</h1>
            <p class="subhead">Share a little about your situation below. The more honestly you write, the more precisely we can meet you where you are.</p>
          </div>

          <form class="request-form" id="request-form">
            <div class="field-row">
              <div class="field">
                <label for="full-name">Full Name</label>
                <input type="text" id="full-name" name="full-name" placeholder="Your name" required>
              </div>
              <div class="field">
                <label for="email">Email Address</label>
                <input type="email" id="email" name="email" placeholder="you@example.com" required>
              </div>
            </div>

            <div class="field">
              <label for="order-ref">Order / Reference Number</label>
              <input type="text" id="order-ref" name="order-ref" placeholder="From your purchase confirmation" required>
            </div>

            <div class="field">
              <label for="main-question">Your Main Question or Situation</label>
              <textarea id="main-question" name="main-question" placeholder="What is the situation you'd like guidance on?" required></textarea>
            </div>

            <div class="field">
              <label for="additional-context">Additional Context <span style="text-transform:none; letter-spacing:normal; color: var(--ivory-500);">(optional)</span></label>
              <textarea id="additional-context" name="additional-context" placeholder="Anything else that would help us understand your situation."></textarea>
            </div>

            <div class="form-foot">
              <span class="form-status" id="form-status"></span>
              <button type="submit" class="btn btn-primary" id="form-submit">Send My Request</button>
            </div>
          </form>
        </div>

      </div>
    </section>
  </main>

  <footer class="site-footer">
    <div class="wrap">
      <span>&copy; 2026 Venicit. All rights reserved.</span>
      <span>Private &amp; Confidential Client Portal</span>
    </div>
  </footer>

  <script>
// Venicit — Private Reading Request Portal
// Access-gate + form-reveal behavior. The password check below is a
// placeholder (verifyAccess) — it is NOT secure as written and is meant
// to be swapped for a real call to a Cloudflare Worker once that's built.
// Do not treat this as the final security mechanism.

(function () {
  "use strict";

  /* Mobile nav toggle */
  var toggle = document.querySelector(".nav-toggle");
  var nav = document.querySelector(".nav-links");

  if (toggle && nav) {
    toggle.addEventListener("click", function () {
      var isOpen = nav.classList.toggle("is-open");
      toggle.setAttribute("aria-expanded", String(isOpen));
    });

    nav.querySelectorAll("a").forEach(function (link) {
      link.addEventListener("click", function () {
        nav.classList.remove("is-open");
        toggle.setAttribute("aria-expanded", "false");
      });
    });
  }

  /* ---------- Access gate ---------- */
  var gateForm = document.getElementById("gate-form");
  var gateEl = document.getElementById("gate");
  var gateError = document.getElementById("gate-error");
  var gateSubmit = document.getElementById("gate-submit");
  var requestPanel = document.getElementById("request-panel");

  // PLACEHOLDER ONLY — replace with a real fetch() to a Cloudflare Worker
  // endpoint (e.g. /api/verify-access) that checks the code server-side
  // and never ships the real secret to the browser.
  function verifyAccess(code) {
    return Promise.resolve(false); // stubbed until the Worker is wired up
  }

  if (gateForm) {
    gateForm.addEventListener("submit", function (e) {
      e.preventDefault();
      var input = document.getElementById("access-code");
      var code = input.value.trim();

      gateError.textContent = "";
      if (!code) {
        gateError.textContent = "Please enter your access code.";
        return;
      }

      gateSubmit.disabled = true;
      gateSubmit.textContent = "Checking…";

      verifyAccess(code).then(function (ok) {
        gateSubmit.disabled = false;
        gateSubmit.textContent = "Unlock My Reading";

        if (ok) {
          gateEl.classList.add("is-hidden");
          requestPanel.classList.remove("is-hidden");
          requestPanel.classList.add("fade-enter");
          requestPanel.setAttribute("tabindex", "-1");
          requestPanel.focus();
        } else {
          gateError.textContent = "That code doesn't match. Please check your purchase confirmation and try again.";
        }
      });
    });
  }

  /* ---------- Reading request form ---------- */
  var requestForm = document.getElementById("request-form");
  var formStatus = document.getElementById("form-status");
  var formSubmit = document.getElementById("form-submit");

  if (requestForm) {
    requestForm.addEventListener("submit", function (e) {
      e.preventDefault();
      // Submission destination (e.g. email API via Worker) to be wired up
      // in a later step. For now this just confirms the intent to the user.
      formSubmit.disabled = true;
      formSubmit.textContent = "Sending…";
      formStatus.removeAttribute("data-state");
      formStatus.textContent = "";

      setTimeout(function () {
        formSubmit.disabled = false;
        formSubmit.textContent = "Send My Request";
        formStatus.setAttribute("data-state", "success");
        formStatus.textContent = "Submission handling isn't connected yet.";
      }, 400);
    });
  }
})();
  </script>
</body>
</html>
